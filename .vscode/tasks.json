{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup_python_env",
            "type": "shell",
            "linux": {
                "command": "set -e; PYTHONPATH=${workspaceFolder}:${PYTHONPATH} /isaac-sim/python.sh - <<'PY' > ${workspaceFolder}/.vscode/.python_samples.env\nimport os\nfor k,v in os.environ.items():\n    # VSCode envFile 형식: KEY=VALUE\n    print(f\"{k}={v}\")\nPY"
            }
        }
    ]
}
